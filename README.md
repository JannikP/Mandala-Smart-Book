# Mandala-Smart-Book

## Build

```bash
cross build --target aarch64-unknown-linux-gnu --release
```

Copy `target/aarch64-unknown-linux-gnu/release/Mandala-Smart-Book` to the Raspberry Pi, make it
executable with `chmod +x Mandala-Smart-Book` and run.
